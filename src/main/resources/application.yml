spring:
  application:
    name: extract-service
server:
  port: 8081
management:
  endpoints:
    web:
      exposure:
        include:
        - '*'

logging:
  level:
    root: warn
    com.thengara.cuckoo: debug
  file: log/app.log
  
